#using generator to produce Fibonacci series.

def Fibonacci(max):
                     a,b=0,1

                     while a <= max:
                                     yield a
                                     a, b = b, a + b

#Function Call
for i in Fibonacci(100):
                           print i,
