#WAP to find the sum of first n natural numbers

#Using for loop

sum=0;
n=input("Enter number of terms whose sum is to determine:");

for i in range(1,n+1,1):
                         sum=sum+i;
                         print sum;

#using while loop

sum1=0
i=1             #i in initialization syep
n1=input("Enter number of terms whose sum is to determine:");

while(i<=n1):   #i in test expression
                         sum1=sum1+i;
                         print sum1;
                         i=i+1         #i in update expression
